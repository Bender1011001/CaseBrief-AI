GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
PROJECT_ID=casebrief-ai-prod
GEMINI_MODEL=gemini-1.5-pro
BUCKET_NAME=casebrief-ai-uploads
# For Firebase: If separate, add FIREBASE_PROJECT_ID, but use service account for all.

Copy to .env and fill in paths/values. Download service account JSON from GCP IAM.
For production, use Cloud Secret Manager instead of local files for credentials.